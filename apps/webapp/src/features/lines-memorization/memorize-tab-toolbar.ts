import type { ToolbarDefinition } from '../../components/toolbar.component';

export const MEMORIZE_TOOLBAR: ToolbarDefinition = {
  items: [
    {
      label: 'search',
      icon: 'search',
    },
  ],
};
